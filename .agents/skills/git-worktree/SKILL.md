---
name: git-worktree
description: Use a separate branch/worktree per active task.
---

# git-worktree

## Instructions
Use a separate branch/worktree per active task. Keep status clean. Conventional Commits. Never push/merge without human approval.

## Always
- Read `AGENTS.md`.
- Use the architecture routing index.
- Do not silently change architecture.
- Run relevant quality gates.
- Never push/merge without human approval.
