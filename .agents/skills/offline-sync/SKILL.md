---
name: offline-sync
description: OperationId + per-device sequence + checkpoint; server authority; same-branch recovery first; conflict resolution is new action.
---

# offline-sync

## Instructions
OperationId + per-device sequence + checkpoint; server authority; same-branch recovery first; conflict resolution is new action.

## Always
- Read `AGENTS.md`.
- Use the architecture routing index.
- Do not silently change architecture.
- Run relevant quality gates.
- Never push/merge without human approval.
