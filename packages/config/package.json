{
  "name": "@commerce-platform/config",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "node -e \"console.log('Nothing to build for @commerce-platform/config (ships tsconfig JSON only)')\"",
    "lint": "eslint typescript --ext .json,.ts --no-error-on-unmatched-pattern",
    "typecheck": "node -e \"console.log('Nothing to typecheck for @commerce-platform/config (ships tsconfig JSON only)')\"",
    "test": "node -e \"console.log('No tests for @commerce-platform/config')\""
  }
}
