{
  "name": "@commerce-platform/contracts",
  "private": true,
  "version": "0.0.0",
  "description": "Shared, dependency-free contract primitives: stable error catalog, API envelopes and DTO schemas.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --root ../.. --config vitest.config.ts packages/contracts/src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
