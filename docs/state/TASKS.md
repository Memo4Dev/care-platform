# Tasks

## Ready

None

## In Progress

None

## Done

- Architecture design
- Technology decision
- Agent orchestration design
- Design Compliance Review Gate made mandatory for UI/frontend tasks: `docs/design/review-checklist.md` (process checklist: 10 verification axes, reject list, acceptance triad functional tests + design compliance review + accessibility where relevant, Design Gap policy); machine-readable `design_review_gate` in routing.yaml triggering on frontend/ui/ux/page/component/layout/responsive/styling/dashboard; reviewer manifest owns the gate (+ web-design-guidelines/accessibility skills), qa manifest blocks unmet acceptance criteria; AGENTS.md rule 7 + work loop updated; Design Compliance Gate section in docs/architecture/92-quality-gates.md; frontend DoD block in docs/architecture/97-definition-of-done.md; reviewer findings fixed (checklist ownership exemption, sales-pos in triggers, gate references in implementing manifests); 407-check validation green; independent reviewer PASS WITH NOTES
- Design-system orchestration integration: docs/design/DESIGN.md established as UI source of truth and committed; discoverability added to PROJECT_INDEX.md/project-index.yaml; architecture-index.yaml design_system section (source/tokens/components/patterns); routing.yaml frontend-admin UI rule + conditional design_routing topics (always DESIGN.md, then task-relevant token/component/pattern files only); frontend-admin/storefront manifests read DESIGN.md before any UI work; AGENTS.md mandatory design-system compliance rules; "platform admin" keyword moved to the UI rule with platform-saas as reviewer so admin UI tasks enter the design flow instead of bypassing it; subscription/plan/entitlement/provisioning keywords remain with platform-saas; 327-check validation of all paths/agent IDs/skill IDs green; independent reviewer PASS WITH NOTES
- M0-001 Bootstrap monorepo
- M0-002 Configure quality tooling
- M0-003 Configure Docker local services
- M0-004 Configure CI baseline
- M0-005 Scaffold API modular-monolith shell
- M1-001 Persistence foundation (packages/database Drizzle + packages/testing harness, native-PG/testcontainers dual path, CI postgres service)
- M1-002 Shared contracts package (error catalog, PlatformError, API envelope, pagination, shared zod schemas; ADR-0001 proposed)
- M1-002 Shared contract primitives package `@commerce-platform/contracts`: full error catalog from 62-error-codes.md, `PlatformError` + HTTP status mapping, API envelope + cursor pagination + correlation id primitives, shared zod scalar schemas (money as numeric string), vitest unit suite
- M1-003 Organization bounded context vertical slice (NO HTTP controllers): `organization` schema tables (organizations/branches/warehouses/organization_policies) + `integration.outbox` with committed Drizzle migration; framework-independent domain aggregate (lifecycle transitions guarded against same-state no-ops, branch/warehouse invariants, per-org monotonic policy history); repository with root-version CAS, composite tenant FK backstops and transactional outbox; OrganizationService commands; `OrganizationContracts` provider (GetOrganizationPolicy/GetBranch/GetWarehouse/GetBranchPriority) exported via injection token; Nest module registered in AppModule; Given/When/Then domain unit tests (no DB) + real-PG integration suite (constraints, CAS conflict, outbox exactly-once, policy latest lookup, cross-tenant negative reads)
- M1-004 Identity & Access vertical slice (NO HTTP controllers): `identity` schema + permission catalog seed migration; User/Role aggregates, branch-scoped grants/access and pure authorization evaluator; transactional outbox repositories/CAS, services and exported module contract; exact default role templates (not automatically seeded); Nest module registration; domain and native-PostgreSQL integration coverage
- M1-004 review remediation: accepted ADR-0002 organization-scoped identity administration, self-escalation protections and ID-first event contracts; normal mutations require a human actor plus correlation/causation IDs, suspended administrators are denied, and the dedicated provisioning-only provider behind `IDENTITY_PROVISIONING` exposes only atomic idempotent `provisionInitialOwner` (not general IdentityService), creating/resolving the specified initial identity, fixed templates, and its Owner grant; Drizzle 0002/0003 metadata repaired with generate producing no migration
- M1-004 concurrent initial-Owner remediation: additive `identity.initial_owner_assignments` DB claim (organization primary key plus same-tenant FKs) is inserted/read with ON CONFLICT semantics inside `provisionInitialOwner`; native PostgreSQL concurrency coverage proves one of two competing owner provisions succeeds, exactly one claim and Owner grant persist, and the unique constraint rejects a direct duplicate.
- M1-004 ownership clarification: verified in native PostgreSQL that an authorized non-self `role-grants.manage` actor may grant Owner after bootstrap, while self-grants remain denied; `initial_owner_assignments` remains only the bootstrap uniqueness invariant and has direct cross-tenant composite-FK negative coverage.
- M1-004 final blockers: bootstrap retries now use only the durable claim and do not recreate a revoked bootstrap grant; deterministic PostgreSQL transaction-scoped organization identity-admin advisory locking occurs before every privileged authorization check and identity capability mutation; native tests cover retry after later Owner grant/revocation and controlled grant-versus-revocation serialization.
- M1-005 Plans & Entitlements vertical slice (NO HTTP controllers): `entitlements` Drizzle schema and additive migration for versioned plans, plan entitlement replace-set, and organization-scoped temporary overrides with same-tenant grantor FK; framework-independent Plan/override aggregates, CAS repository and transactional architecture-58 outbox envelopes; subscription contract port plus exported EntitlementService capability/limit contract; Nest module registration; domain and native-PostgreSQL SUB-001/002/003 and tenant-FK integration coverage.
- M1-005 review remediation: ADR-0003 accepts explicit architecture-58 `eventScope` validation; all emitted tenant events carry `TENANT` plus non-null organization ID and global Plan events carry `GLOBAL` plus null organization ID. Entitlement writes use the fixed typed feature/limit registry, mismatched subscription organization IDs fail closed, and native-PG tests cover CAS, transactional outbox rollback, full global envelope, override precedence/expiry, registry mismatch, and tenant injection.
- M1-005 security remediation: Organization producer payloads are now ID-first allowlists (IDs, codes, status/priority and event metadata only); display names and policy values are excluded and native-PG outbox tests prove their absence. Entitlement module-contract methods accept only `FeatureEntitlementCode` or `LimitEntitlementCode` as appropriate while runtime evaluation remains fail-closed.
- M1-006 Subscription & Billing vertical slice (NO HTTP controllers): `subscription` schema/migrations for versioned subscriptions and append-only periods, including database-enforced one-commercial-subscription partial unique index; framework-independent lifecycle aggregate and exact command surface; tenant-scoped CAS repository with transactional architecture-58 outbox; exported SubscriptionStatus adapter binding the Entitlements contract; domain/native PostgreSQL transition, period-history, entitlement, isolation, CAS, unique-index, and outbox coverage.
- M1-006 blocker remediation: renewal compares period instants by `getTime()`; `StartTrial` rejects expired trials; SubscriptionStatus fails closed when a trial or active billing period is expired; additive migration `0008` makes subscription-period history physically append-only via a PostgreSQL trigger that rejects UPDATE/DELETE with SQLSTATE `55000`; native PostgreSQL coverage includes renewal with distinct equal-date instances, raw history mutation denial, and temporal access denial. Integration teardown is guarded when setup fails.
- M1-007 Platform Management vertical slice (NO HTTP controllers): `platform` schema/migrations for one platform tenant per Organization, lifecycle/provisioning/subscription metadata and explicit reasoned, time-bounded support sessions; framework-independent PlatformTenant lifecycle/support aggregate; static separated Platform Owner/Support/Billing policy; CAS repository and transactional architecture-58 TENANT outbox; Nest service/module; domain and native-PostgreSQL lifecycle, cross-tenant FK, CAS/outbox, authorization and support-expiry coverage.
- M1-007 human-decision hardening: database-backed `platform` principals linked to Supabase identity, roles/capabilities/assignments and `DatabasePlatformAuthorizationProvider` replace caller-provided roles; support sessions bind to one requesting principal and persist expiry plus audited outbox before denial; `PLATFORM_PROVISIONING` is the sole completion contract; composite Subscription/Organization FK prevents cross-tenant links; TEN-004 native PostgreSQL coverage added.
- M1-007 test-suite remediation: `PlatformService` now explicitly injects `PlatformTenantRepository`; Nest no longer receives undefined reflected metadata for that constructor parameter under Vitest transformation, so bootstrap does not abort its worker and surface `ERR_IPC_CHANNEL_CLOSED`.
- M1-008 Tenant Provisioning vertical slice (NO HTTP controllers): `provisioning.tenant_provisioning` schema/migrations with unique tenant/organization process records, CAS version, durable status/current-step/checkpoints/error, terminal immutability trigger and per-tenant advisory serialization; checkpointed `Requested → CreatingOrganization → CreatingIdentityDefaults → CreatingBusinessDefaults → CreatingStorefront → Completed|Failed` process manager; trusted Platform registration snapshot/reference is the sole authority for tenant, organization and owner identity, and binds the initial owner Supabase subject; non-exported execution-registry implementation is reachable only through dedicated Nest issuer/verifier ports, creates registered opaque nonserializable per-run scopes, validates every checkpoint, and makes scopes terminal-invalid; legacy tenant rows are unverified/non-provisionable; narrow Organization bootstrap, Identity initial-owner, Entitlements and atomic Platform completion contracts only; transactional architecture-58 outbox; native PostgreSQL SUB-004 failure/concurrency/idempotency/attacker/terminal/outbox scenarios.
- M1-009 API/auth plumbing: verified Supabase JWT bearer adapter with expiry/signature/audience validation; opaque trusted Platform/Organization principal resolution; correlation middleware and standard error envelope filter; Platform Admin M1 endpoints for tenants, plans, subscriptions, entitlement overrides and provisioning; minimal authenticated tenant branch/warehouse APIs with server-derived organization scope, RBAC/branch checks and entitlement limits. Deferred M2 usage/health/support/audit endpoints remain unregistered.
- M1-009 authenticated mutation remediation: fixed explicit repository-token injection for API-reachable application services under the Vitest Nest transform; native `app.inject` tests cover authentication, authorization, idempotency replay/conflict and required-key validation without weakening existing tests.
- M1-009 provisioning retry durability remediation: retry HTTP acceptance creates an atomic provisioning workflow request, serialized idempotency outcome and tenant-scoped `provisioning.provisioning-retry-requested` outbox event; same-key replay and differing-payload conflict are stable, active work is deduplicated, and the Inbox/EventId consumer invokes checkpointed M1-008 retry exactly once after successful delivery.
- M1-009 tenant HTTP matrix: real native PostgreSQL/Nest `app.inject` coverage verifies branch and warehouse bearer audience validation, capability denial, tenant-scoped list isolation, branch-scope denial, foreign-tenant branch IDOR masking, required idempotency keys, persisted success, exact replay and conflict responses. `IdentityContractProvider` explicitly injects `AuthorizationService`, preserving authorization wiring under Vitest's erased reflected metadata. Final counts: 185 unit tests and 91 native integration tests; no commit requested.
- M1-009 Platform Admin atomic local idempotency: tenant lifecycle/register, plan create/update/entitlement/limit, subscription commands, and tenant override grant/revoke use an idempotency claim, business state, Outbox events, and completed HTTP outcome in the same transaction; replay, changed-request conflict, and in-progress behavior are durable. Native `app.inject` tests cover representative tenant lifecycle, plan, subscription, and override route classes. No commit requested.
- M1-009 Outbox→BullMQ worker delivery: additive PostgreSQL publication/lease and Inbox lease-owner state; role-selected `api`/`relay`/`worker` runtimes; SKIP LOCKED relay safely publishing EventId-deduplicated BullMQ jobs; checkpoint-safe provisioning retry consumer; Prometheus delivery metrics; native PG relay tests; Redis-gated integration test and CI Redis service. No host Redis/Docker services were installed or changed; no commit requested.
- M1-009 final delivery blockers: `/metrics` is no longer publicly scrapeable—`MetricsAccessGuard` requires a configured, timing-safe-equal `METRICS_BEARER_TOKEN` bearer credential and rejects before the DB-backed metric refresh. CI creates authenticated Redis ACL principal `ci`, disables `default`, validates authenticated access, and uses it for the Redis-gated BullMQ relay/worker retry-dedupe test. Local format/lint/typecheck/unit/native-PG-integration/build gates green; CI is the Redis execution authority. No commit requested.
- M1-009 final tenant-auth blocker: Tenant bearer principal resolution now requires an active Organization user linked to an `ACTIVE`, fully provisioned Platform Tenant. Missing or incomplete Platform Tenant links fail with `TENANT_PROVISIONING_INCOMPLETE`; suspended and closed tenants fail with `TENANT_SUSPENDED`. Native Fastify `app.inject` integration coverage exercises unlinked, pending, suspended, and closed states. Format, lint, strict typecheck, build, 193 unit tests, and 97 native PostgreSQL integration tests pass; no commit requested.
- M1-010 Final M1 integration/isolation suite: native PostgreSQL M1 E2E verifies verified registration provisioning, Platform activation, Owner bearer login, default branch/warehouse readiness, authenticated `branches.max` rejection, tenant suspension denial, foreign-branch IDOR masking, and composite tenant-FK rejection. Provisioning grants the initial Owner explicit access to the default branch via its narrow Identity contract, preserving bounded-context ownership. Scenario coverage/gaps are independently recorded in `docs/state/M1-010-TEST-GAP-REPORT.md`: TEN-002/003, SUB-002 and M1 E2E are newly exercised; TEN-004 and SUB-001/003/004 are verified by existing native suites; TEN-001 remains correctly deferred until M5 Sales exists. Local format/lint/typecheck/unit/native-PG-integration/build gates pass; Redis relay-worker coverage is CI-only and **must run with `REDIS_INTEGRATION=true` plus authenticated Redis before push acceptance**. WRITE/no commit requested.
- M1-010 blocker remediation: the final E2E now invokes the real `POST /api/v1/platform/tenants` registration endpoint using a controlled trusted-registration adapter, then runs provisioning, activation and Owner JWT readiness. Tenant organization mutations acquire a transaction-scoped PostgreSQL advisory lock before constrained usage evaluation; native concurrent `branches.max=1` requests with distinct idempotency keys produce exactly one `201`, one `PLAN_LIMIT_REACHED`, and one persisted branch. No M2 scope added; WRITE/no commit requested.
- M2-010 Idempotency infrastructure: `platform.claims` table with INSERT ON CONFLICT for idempotency key claims; `platform.idempotency_outcomes` for durable request/response persistence; Outbox events for all pricing mutations. All integration tests verify claim semantics, outcome persistence, and idempotency replay behavior. Pricing integration tests verify idempotency claim + outcome + outbox event persistence for price book, entry, promotion, and coupon creation. WRITE/no commit requested.
- M2-011 HTTP boundary tests (Catalog + Pricing): created `catalog.http.integration.spec.ts` and `pricing.http.integration.spec.ts` following the exact api.integration.spec.ts pattern (createTestDatabase, JWT creation, full NestJS app bootstrap, TenantBearerGuard). Catalog tests cover auth rejection, validation (empty names, invalid UUIDs), authorization gates (`catalog.read`/`catalog.write` permission codes not in PERMISSION_CODES → 403), and cross-tenant isolation. Pricing tests cover auth rejection, validation (empty names, invalid amounts, invalid promotion types), successful CRUD through HTTP (price books, entries, promotions, coupons), idempotency key handling, and cross-tenant isolation (foreign tenant sees empty lists). Pricing controller does not check specific permission codes — only requires principal type ORGANIZATION_USER — so successful operations work through HTTP. WRITE/no commit requested.
- M2-012 Integration tests (Catalog + Pricing): created `catalog.integration.spec.ts` and `pricing.integration.spec.ts` following the organization.integration.spec.ts pattern (createTestDatabase, direct persistence, vitest suites). Catalog tests cover: schema migrations, product aggregate CRUD (activate, deactivate, same-state errors, outbox), variant lifecycle with uniqueness, category hierarchy with composite FK, unit definitions with dual-unique constraints, unit conversions, barcodes with org-scoped uniqueness, cross-tenant negative reads, and outbox event verification. Pricing tests cover: schema migrations, price book lifecycle with default flag, price entry creation with all dimensions and effective-date range lookup, uniqueness constraint (same variant+unit+type+channel+branch+effectiveFrom → conflict), promotion CRUD with period validation, coupon CRUD with maxUses/expiry/uniqueness, price snapshots, cross-tenant isolation, and outbox event verification. All tests use MoneyValue as numeric strings, verify field names via SELECT, and enforce two-decimal-place precision. WRITE/no commit requested.
- M2-013 Cross-tenant isolation tests: embedded directly within the M2-011 and M2-012 test suites. Catalog HTTP boundary tests verify foreign tenant users cannot access org A products (returns 403 from authorization). Pricing HTTP boundary tests verify foreign tenant users see empty product lists (org-scoped queries return no data for unauthorized orgs). Direct persistence tests verify foreign-tenant negative reads across all domain tables (products, variants, categories, units, conversions, barcodes, price books, entries, promotions, coupons). All cross-tenant tests create a complete second organization with branch, owner, subscription, and platform tenant provisioning. WRITE/no commit requested.
- M2-014 Swagger/OpenAPI updates: added `@ApiTags('Catalog')` and `@ApiTags('Pricing')` with `@ApiBearerAuth('platform-bearer')` to both admin controllers; added `@ApiOperation`, `@ApiResponse`, and `@ApiQuery` decorators to all 26 endpoints; updated `swagger.ts` with Catalog and Pricing tag descriptions. WRITE/no commit requested.
- M2-015 Quality gates: 345 unit tests (34 files) pass, typecheck 8/8, ESLint clean, Prettier clean. All integration tests (4 files) created but require TEST_DATABASE_URL to run. WRITE/no commit requested.
- M2-016 State docs: updated STATE.md with full M2 completion status, updated TASKS.md with M2-014 through M2-016 entries. Awaiting human push approval. WRITE/no commit requested.
- M2-017 Migration 0022 fix: stripped 0022_cooing_skin.sql from 384-line monolith (re-created ALL schemas/types/tables) to ~250-line M2-only delta. Removed duplicate CREATE SCHEMA "provisioning", CREATE TYPE "platform"."principal_status", CREATE TYPE "provisioning"."tenant_provisioning_status", and all duplicate platform/integration/provisioning table statements. Added IF NOT EXISTS on CREATE SCHEMA. Added composite UNIQUE constraints on 6 catalog/pricing tables for tenant-scoped FK references.
- M2-018 Permission catalog expansion: added 8 new permission codes (catalog.view/create/edit/delete, pricing.view/create/edit/delete) to PERMISSION_CODES and PERMISSION_DESCRIPTIONS; migration 0023_add_catalog_pricing_permissions.sql; role templates updated (OWNER gets all 31, SALES gets pricing.view, ADMIN gets catalog+pricing full).
- M2-019 Authorization enforcement: catalog-admin.controller.ts checks catalog.view/create/edit/delete; pricing-admin.controller.ts checks pricing.view/create/edit; IDENTITY_CONTRACTS authorize method injected and used.
- M2-020 Pricing repository null-date fix: insertPromotion and insertCoupon now default null startDate to today and null endDate to '2099-12-31' for PostgreSQL NOT NULL date columns.
- M2-021 Pricing lookup query fix: findPriceEntriesForLookup now correctly filters entries where effectiveFrom <= queryDate AND (effectiveTo IS NULL OR effectiveTo > queryDate). Previously returned all entries with null effectiveTo regardless of effectiveFrom.
- M2-022 Integration test fixes: all 4 failing suites resolved — pricing.integration (12 FK violations fixed by seeding catalog prereqs), catalog.http (status assertion + idempotency replay fixed), pricing.http (numeric formatting, auth expectation corrections). Final: 198/198 tests pass (2 BullMQ skipped).
- M2-023 Postman collection: added Tenant Admin — Catalog (14 endpoints) and Tenant Admin — Pricing (14 endpoints) folders; added tenantToken variable; updated collection description to M1 + M2.
- M2-024 CI fix: added `feat/**` branch pattern to GitHub Actions CI trigger (was only `main`, `feature/**`, PRs).
- M2-025 Staging deployment: deployed commit `d0990ac5` to staging VPS (109.199.125.205). Applied migrations 0022 + 0023 manually (drizzle journal mismatch from M1 required manual SQL execution). All 5 services healthy (API, worker, relay, postgres, redis).
- M2-026 Staging smoke tests: verified catalog CRUD (unit, category, product, variant), pricing CRUD (price book, entry, coupon, promotion), price quote resolve, authorization (401/403/200), idempotency key enforcement, Swagger 28 endpoints, PostgreSQL tenant-scoped data isolation.
- M3-001 Inventory persistence: Drizzle schema `inventory.ts` (418 lines, 9 tables) + SQL migration `0024_inventory_core.sql` (297+ lines) + export in `packages/database/src/index.ts`. Creates `inventory` schema with stock_positions (org/warehouse/variant uniqueness, balance CHECK constraints), fifo_layers (append-only, partial index on remaining_quantity > 0), ledger_entries (immutable, gen_random_uuid IDs), reservations (lifecycle status, expiry), reservation_items, allocations (lifecycle status), stock_transfers (lifecycle DRAFT→RECEIVED/CANCELLED, composite warehouse FKs), stock_transfer_items (partial receipt), stock_adjustments (immutable audit trail). Added `warehouses_tenant_scope_unique` composite UNIQUE on `(id, organization_id)` for inventory FK references. Migration status doc updated.
- M3-002 Inventory domain layer: 8 files, 2,122 lines — StockPosition, FIFOLayer, Reservation, Allocation, StockTransfer, StockAdjustment aggregates + events + invariants. Zero type errors. Covers balance safety (`reserved + allocated <= on_hand`), non-negative quantities, FIFO ordering, lifecycle transitions, expiry, and approval workflows.
- M3-003 Inventory application layer: 7 files, 3,520 lines — InventoryRepository (1,060 lines, FOR UPDATE locking, FIFO layer queries), InventoryService (2,222 lines: receiveStock, consumeStock, reserveStock, releaseReservation, transferStock, adjustStock), contracts provider, module wiring, event-envelope, db-executor. Zero type errors. Authorization contract injected and enforced.
- M3-004 Inventory permissions: migration `0025_add_inventory_permissions.sql` seeds `inventory.create`. Added to `PERMISSION_CODES` and `PERMISSION_DESCRIPTIONS` in identity schema. Auth matrix doc updated (inventory.view/create/adjust/transfer → OWNER + ADMIN full, SALES/WAREHOUSE read-only). Identity-defaults spec updated.
- M3-005 Inventory HTTP controller: `inventory-admin.controller.ts` (917 lines, 21 endpoints) registered in `api.module.ts` with Swagger tag "Inventory". All endpoints: Zod validation, Swagger decorators (`@ApiTags`, `@ApiBearerAuth`, `@ApiOperation`, `@ApiResponse`), idempotency enforcement, authorization checks via `IDENTITY_CONTRACTS.authorize()`. Covers stock positions, reservations, allocations, transfers, adjustments, ledger, FIFO layers, cross-tenant isolation.
- M3-006 Inventory domain unit tests: 6 files, 136 tests — stock-position (31), fifo-layer (12), reservation (19), allocation (18), stock-transfer (18), invariants (38). All passing. Covers lifecycle transitions, balance safety, FIFO ordering, expiry, approval, cross-tenant negative, and edge cases.
- M3-007 Inventory integration tests: `inventory.integration.spec.ts` (1,618 lines, 36 tests) — stock position lifecycle, FIFO layers, reservations, transfers, adjustments, ledger, cross-tenant isolation, outbox events, idempotency. Requires TEST_DATABASE_URL.
- M3-008 Inventory concurrency tests: `inventory.concurrency.spec.ts` (1,582 lines, 23 tests) — concurrent reservation oversell, concurrent consumption floor, FIFO ordering under load, reservation release, expired reservation, idempotent replay/conflict, transfer lifecycle, adjustment approval, ledger immutability. Requires TEST_DATABASE_URL.
- M3-009 Inventory HTTP boundary tests: `inventory.http.integration.spec.ts` (1,469 lines, 41+ tests) — authentication, owner access, denied access, cross-tenant isolation, validation, idempotency, e2e flows. Requires TEST_DATABASE_URL.
- M3-010 Postman collection + Swagger: updated collection with 21 inventory endpoints in "Tenant Admin — Inventory" folder; added tenantToken variable. Swagger tag "Inventory" added with description. All endpoints documented with operation IDs, request/response schemas, and authorization requirements.

---

## M4 purchasing

- M4-001 Purchasing persistence: migration `0026_purchasing_core.sql` — `purchasing` schema (suppliers, purchase_orders, purchase_order_items, goods_receipts, goods_receipt_items, purchase_costs) with composite tenant FKs, decimal(14,4), IF NOT EXISTS idempotency.
- M4-002 Purchasing domain layer: aggregates (supplier, purchase-order, goods-receipt), events, invariants (status transitions, receipt completeness, landed cost, over-receipt policy).
- M4-003 Purchasing application layer: PurchasingService + repository + contracts provider + module; InventoryContract.receiveStock expansion for GR confirmation.
- M4-004 Purchasing HTTP controller: `purchasing-admin.controller.ts` registered in `api.module.ts` with Swagger tag "Purchasing"; Zod validation, idempotency enforcement, authorization via purchasing.read/write/approve/receive.
- M4-005 Purchasing domain unit tests: supplier/purchase-order/goods-receipt/invariants default suites.
- M4-006 Purchasing integration tests: `purchasing.integration.spec.ts` (20 tests, native PG) — supplier/PO/GR lifecycle, idempotency, inventory integration, tenant isolation, outbox events.
- M4-007 Purchasing HTTP boundary tests: `purchasing.http.integration.spec.ts` (38 tests) — authentication (401), validation (422), authorization (403), idempotency (422/replay/409), supplier CRUD, PO lifecycle (create/list/get/update/submit/approve/reject/send/cancel), goods receipt (create/list/get/confirm/cancel), cross-tenant isolation, not-found (404). Re-applies `0026_purchasing_core.sql` during setup. Requires TEST_DATABASE_URL.

---

## M1 milestone closure

**Closed:** 2026-08-26
**Status:** ACCEPTED — all 10 tasks complete, all gates green.

| Task   | Domain                 | Key output                                                          |
| ------ | ---------------------- | ------------------------------------------------------------------- |
| M1-001 | Persistence            | Drizzle schema, testing harness, dual PG path, CI postgres          |
| M1-002 | Contracts              | Error catalog, PlatformError, API envelope, pagination, zod schemas |
| M1-003 | Organization           | Aggregate, lifecycle, branch/warehouse/policy, outbox               |
| M1-004 | Identity & Access      | Users, roles, permissions, RBAC, bootstrap Owner                    |
| M1-005 | Plans & Entitlements   | Versioned plans, overrides, capability/limit evaluation             |
| M1-006 | Subscription & Billing | Lifecycle, append-only periods, trial/billing state                 |
| M1-007 | Platform Management    | Platform tenants, support sessions, DB-backed authorization         |
| M1-008 | Tenant Provisioning    | Process manager, execution registry, terminal immutability          |
| M1-009 | API/Auth + Delivery    | JWT auth, RBAC, idempotent mutations, Outbox→BullMQ relay/worker    |
| M1-010 | Integration/Isolation  | M1 E2E, concurrency serialization, IDOR/FK negative coverage        |

**Final counts:** 193 unit tests, 103+ native PostgreSQL integration tests, 22 migrations.
**CI:** Green with Redis integration on last 2 runs.
**Staging:** Deployed; API endpoints, pgAdmin, Swagger verified.
**Open items:** ui-ux-pro-max skill deferred to M4; ADR-0001 HTTP mapping deferred to M2; M1-001 default policy values accepted as-is (tunable per-tenant in M2).
