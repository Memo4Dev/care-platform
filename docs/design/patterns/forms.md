# Form pattern

Use a clear record heading, tabs for major sections, and grouped field sections. Pair the main form with a narrow contextual summary only when it helps a decision, such as product stock or order totals.

The primary save action stays in the top bar. Validate inline, preserve unsaved changes, and explain irreversible actions before confirmation.
