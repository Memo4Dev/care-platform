# Data table pattern

Place search, filters, and export or create actions above the table. Keep column controls and bulk operations in the table header.

Default sorting must have a business rationale, such as inventory risk. Status cells use a dot plus written label. Preserve filters when navigating into and back from a record.
